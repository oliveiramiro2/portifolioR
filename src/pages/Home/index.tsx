import React from "react";
import Intro from "../../components/Intro";

const Home: React.FC = () => {
    return (
        <div className="w-screen h-screen">
            <Intro />
        </div>
    );
};

export default Home;
